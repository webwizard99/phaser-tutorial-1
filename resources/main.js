import Phaser from 'Phaser';
import { config, game, preload, create, update } from './phaserBase';
const main = (function(){
  const config = config;
  const game = game;
  const preload = preload;
  const create = create;
  const update = update;

  return {
    
  }
}());

export default main;